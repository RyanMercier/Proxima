# BLOCKERS

## 0. Camera-ready figure set reduced to fit the 10-page limit

The mechanical llncs port of the submitted paper compiled to 17 pages and
reached 21 after the reviewer-requested additions; the hard 10-page limit
(including figures and references) forced the cuts logged in CUTS.md. Two
of those interact with explicit instructions:

- Fig. 4 (fast-path heatmap): the instruction "Update Fig. 4's caption to
  say lower bound; the plotted data is unchanged" presumed the figure
  stays. After every other sanctioned cut the paper was still over the
  limit, and this figure plots only the closed-form (1-p_miss)^N, so it was
  the least destructive remaining cut. The lower-bound framing and the
  60%/90% anchor points are stated in the Sect. 5 fast-path paragraph
  (which Phase C rewrote anyway). The original PNG and a compact
  single-column variant (fig4_fast_path_compact.png, generated by
  visualize.py --compact) remain in figures/; re-adding it costs about half
  a page.
- The protocol-flow figure is displayed cropped to its three Proxima
  columns (fig9_protocol_flow_proxima.png, left 1655 of 2200 px); the
  removed HotStuff strip duplicates the introduction's new HotStuff
  description (B9). The crop is mechanical (PIL), the master PNG is
  untouched, and the label edits below still apply to the master. If the
  master is re-exported per item 1, regenerate the crop with:
  python3 -c "from PIL import Image; im = Image.open('figures/fig9_protocol_flow.png'); im.crop((0, 0, 1655, im.size[1])).save('figures/fig9_protocol_flow_proxima.png')"

## 1. Fig. 2 (fig9_protocol_flow.png) must be re-exported by hand

`fig9_protocol_flow.png` is the only paper figure with no generation script:
`visualize.py` produces fig1 through fig8, and no commit in the repository
history contains a generator for fig9. The PNG (2200 x 1280) was drawn in an
external diagramming tool, so per the camera-ready instructions it is not
redrawn here. The caption and body text in `proxima_llncs.tex` already
describe the corrected fast-path mechanism; until the PNG is re-exported,
three drawn labels inside the bitmap lag the text.

Exact edits to make in the original drawing, then re-export at the same
resolution and margins (PNG, ~2200 x 1280, white background):

1. Box "Send digest + bloom" (Phase 1, Validators column), currently
   "64 B digest / 25 B bloom filter": add a third line so it reads
   "64 B digest / 25 B bloom filter / 96 B BLS commitment if local state
   matches proposal".
2. Decision node (Phase 1, Aggregator column), currently "decision /
   cluster var ~ 0?": change the condition line to
   ">= 2N/3 matching commitments collected?".
3. Fast-path terminal (State column), currently "Finalize in one round trip /
   no Phase 2, no BLS aggregation": change the second line so it reads
   "Finalize in one round trip / aggregate Phase 1 commitments:
   96 B aggregate + N/8 B signer bitmap". The node should read as producing
   the same certificate object as the Phase 2 "Block finalized" node, not a
   separate unsigned finalization.

No other element changes. The "yes, fast path" / "no, proceed to Phase 2"
edge labels, the Phase 2 column, and the HotStuff comparison column stay as
they are. After re-export, overwrite `paper/figures/fig9_protocol_flow.png`
(and any master copy kept outside the repository), recompile, and re-check
the page count.

## 2. Note: "Fig. 9 CSVs" do not exist

The instructions reference regenerating "Fig. 9's bandwidth panel and its
CSVs". The repository's figure pipeline has no CSVs: `visualize.py
figure3()` generates fig3_byzantine_sweep.png (paper Fig. 9) directly from
an in-process simulation with a fixed seed. The figure was regenerated by
running that script after the Phase 1 payload change; nothing was
hand-edited. Verified under a pinned-clock harness that the code change
leaves success rates, message counts, and tree/HotStuff bandwidth
bit-identical, and raises flat bandwidth by exactly 96 B per matching
validator.

## 3. Note: simulation keeps the unanimity engagement gate

The corrected protocol triggers the fast path at >= 2N/3 matching
commitments. In the benchmark simulation, partial observation is drawn
per-validator (Binomial), so a literal 2N/3 gate would engage in a fraction
of trials at 0-10% Byzantine (e.g. ~23% of trials at 0% Byzantine, N=100,
p_miss=0.37) and would change the reviewed message-panel data of Fig. 9,
which the camera-ready must not do. `blockchain.py` therefore keeps the
conservative unanimity gate (cluster variance ~ 0, now additionally requiring
the 2N/3 signed commitments) for engagement, which corresponds exactly to
the (1-p_miss)^N lower bound reported in the paper; the certificate itself
is the corrected signature-backed object in all cases. This is documented in
code comments; no reviewed number changes.
